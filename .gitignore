# Secrets — webhook URLs, credentials
.env

# Persistent Edge profile — contains live login session cookies
edge_profile/

# Legacy Playwright session state
zwr_session.json

# Per-machine scraper state (already-posted event IDs)
posted_zwiftracing_ecro.txt

# Local Claude Code project settings
.claude/

# Python
__pycache__/
*.pyc
